.side-bar
  .header
    %h3.header__name
      = current_user.name
    %ul.header__menu
      %li
        = fa_icon "edit"
      %li
        = link_to edit_user_path(current_user) do
          = fa_icon "cog"
  .groups
    %ul
      %li.group
        =link_to "#", class: "group__link" do
          .group__name
            TestGroup
          .group__last-message
            最後のメッセージはあいうえお
